@using InplayBet.Web.Models
@using InplayBet.Web.Utilities
@model List<BetModel>
@{
    ViewBag.Title = "Latest Bets";
}
@{Html.RenderPartial("_PageTitle", new ViewDataDictionary { { "Caption", "Latest Bets" } });}
<div class="main-container">
    <div class="container">
        <div class="mp-block">
            @{Html.RenderPartial("_RecentBets", Model);}
            @{Html.RenderPartial("_PromoBanner");}
        </div>
    </div>
</div>
